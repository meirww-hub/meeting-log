"""סיכום איכותי + חילוץ TO DO מתוך התמלול, באמצעות Gemini API (Google AI Studio)."""

import math
import re

from google import genai
from google.genai import types
from pydantic import BaseModel

from app.config import settings
from app.models import TodoItem, TranscriptSegment
from app.pipeline._retry import call_with_retry

from app.pipeline._model import GEMINI_MAX_OUTPUT_TOKENS as _MAX_OUTPUT_TOKENS
from app.pipeline._model import GEMINI_MODEL as _MODEL


class _MeetingSummary(BaseModel):
    title: str
    summary: str
    todos: list[TodoItem] = []


_SYSTEM_PROMPT = """\
אתה עוזר שמסכם פגישות ומחלץ מהן משימות לביצוע. תענה תמיד בעברית תקנית,
תמציתית וברורה. פלט המשימות חייב להיות JSON תקני בלבד, ללא טקסט נוסף.

זה נכון גם כשהתמלול עצמו אינו בעברית: התמלול נשמר בשפה שבה השיחה נאמרה
בפועל (ראה transcription.py), ולכן הוא עשוי להגיע באנגלית או משולב עברית
ואנגלית. הכותרת, הסיכום והמשימות תמיד בעברית - תרגם את התוכן, אל תעתיק
אותו כלשונו.
"""

_TASK_SCHEMA_HINT = """\
החזר אובייקט JSON יחיד במבנה הבא בדיוק:
{{
  "title": "כותרת קצרה (2-5 מילים) שמתארת את נושא הפגישה",
  "summary": "סיכום הפגישה מחולק לנושאים ממוספרים, למשל:\\n1. <כותרת נושא קצרה>: <תיאור קצר>\\n2. <כותרת נושא קצרה>: <תיאור קצר>\\nוכן הלאה - כל נושא בשורה נפרדת",
  "todos": [
    {{"description": "...", "owner": "שם האחראי לביצוע, או null", "due_date": "YYYY-MM-DD או null"}}
  ]
}}
אם לא ניתן לזהות תאריך יעד למשימה מסוימת - החזר null בשדה המתאים. לגבי
owner יש סעיף ייעודי למטה.
היום הוא {today}.

לגבי summary: חובה לחלק את הסיכום לנושאים ממוספרים (1, 2, 3 וכו'), כשכל
נושא מתחיל במספר הנושא ואחריו נקודה (למשל "1. ..."), בשורה נפרדת.
אל תכתבו סיכום כפסקאות רציפות ללא מספור.

לגבי היקף הסיכום: אורך הסיכום חייב להיות פרופורציונלי לאורך השיחה ולכמות
הפרטים שנאמרו בה. התמלול שלפניכם מכיל {word_count} מילים, ומתוכן {figure_count}
מילים שמכילות נתון מספרי. לכן נדרשות שתי דרישות מינימום:
  • לפחות {min_topics} נושאים ממוספרים.
  • לפחות {min_words} מילים בסיכום כולו.

שתי הדרישות הן רצפה ולא תקרה, והן מחייבות. לפני שאתם מחזירים את התשובה,
ספרו ובדקו שהסיכום עומד בשתיהן; אם הוא קצר מדי - הרחיבו אותו והוסיפו את
הפרטים הקונקרטיים שהשמטתם.

שיחה ארוכה ועשירה בפרטים מקבלת סיכום מקיף שנכנס לעומק של כל נושא - אל
תסתפקו במשפט אחד לנושא כששוחחו עליו לאורך זמן. **אסור להשמיט נושא שנדון
בשיחה**: אם יש הרבה נושאים, אחדו נושאים קרובים לסעיף אחד וכתבו סעיף ארוך
יותר - אל תמחקו אותם. בכיוון ההפוך, אל תמתחו סיכום של שיחה דלה בתוכן ואל
תחזרו על אותו מידע פעמיים רק כדי להגיע ליעד.

לגבי פרטים מספריים - קריטי: כל מספר משמעותי שנאמר בשיחה חייב להופיע
בסיכום, במדויק כפי שנאמר - סכומי כסף ומחירים (כולל המטבע), כמויות, תאריכים
ושעות, מועדי יעד, אחוזים, משכי זמן, גילאים, מספרי דירה/בית וכתובות, מידות,
מספרי טלפון ומספרים מזהים.
- אל תעגלו ואל תכתבו בקירוב: "12,450 ₪", לא "כ-12 אלף ₪".
- אל תמציאו, אל תשלימו ואל תשערו מספר שלא נאמר בשיחה. אם מספר לא נשמע
  ברור בתמלול - פשוט אל תכללו אותו.
- אם יש התנגשות בין יעד האורך שלמעלה לבין פרט מספרי - הפרט המספרי מנצח.
  עדיף סיכום מעט ארוך מהיעד מאשר סיכום שהשמיט נתון קונקרטי.
- זה חל גם על todos: השאירו בתיאור המשימה את הסכום/הכמות/המועד המדויקים
  שנאמרו.

התמלול שלפניכם הוא זרם טקסט רציף בלי תיוג דוברים - אין לו מידע על מי אמר
מה. אל תמציאו ייחוס אמירה לאדם ספציפי ואל תניחו מי דיבר; תארו החלטות,
בקשות ועמדות כפי שהן, בלי לצרף להן שם, אלא אם השם נאמר במפורש בתוך התוכן
עצמו (למשל "אני, דנה, אעביר את זה").

לגבי owner - האחראי לביצוע המשימה: זו עמודה נפרדת ברשימת המשימות שהמשתמש
מקבל, והיא הדבר הראשון שמסתכלים עליו בה - משימה בלי אחראי היא משימה שאיש
לא ייקח. לכן מלאו owner רק כשהשם נאמר במפורש בתוכן עצמו - למשל מישהו
מזכיר את שמו ("אני, דנה, אעביר את זה"), פונה לאדם בשמו ("דנה, תכיני את
הדוח"), או מזכיר שם של מי שאחראי גם אם אינו דובר בשיחה ("שיוסי מהנהלת
חשבונות יוציא חשבונית"). החזירו null כשאין בתוכן שם מפורש לאחראי - זו
ברירת המחדל, לא חריג. אל תנחשו ואל תמציאו שם שלא נאמר בשיחה. אם המשימה
משותפת לכמה אנשים ששמם נאמר - כתבו את כולם מופרדים בפסיק.
"""


def _format_transcript(segments: list[TranscriptSegment]) -> str:
    return "\n".join(s.text for s in segments)


_FIGURE_RE = re.compile(r"\d")


def _word_count(segments: list[TranscriptSegment]) -> int:
    return sum(len(s.text.split()) for s in segments)


def _figure_count(segments: list[TranscriptSegment]) -> int:
    """כמה מילים בתמלול מכילות ספרה - מדד לצפיפות הנתונים בשיחה.

    שתי שיחות באותו אורך יכולות להיות שונות לגמרי בכמות המידע הקונקרטי:
    ישיבת הנהלה עם עשרות סכומים ותאריכים צריכה יותר מקום מפטפוט באותו אורך,
    אחרת המספרים פשוט לא נכנסים לתקציב ונופלים."""
    return sum(1 for s in segments for w in s.text.split() if _FIGURE_RE.search(w))


def _summary_budget(word_count: int, figure_count: int) -> tuple[int, int]:
    """מחזיר (מינימום נושאים, מינימום מילים בסיכום) - שתי רצפות, לא תקרות.

    היעד מחושב בקוד ולא מושאר לשיקול המודל, כי מודלי שפה מתכנסים לאורך פלט
    קבוע כשלא נותנים להם מספר מפורש - הנחיה מילולית ("סכם באופן
    פרופורציונלי") מייצרת את אותו סיכום בן שלוש שורות גם לשיחה של דקתיים
    וגם לפגישה של שעתיים.

    הסקאלה תת-לינארית (שורש): שיחה ארוכה פי 10 מקבלת סיכום ארוך פי ~3 בלבד,
    כי יחס הדחיסה הטבעי גדל עם אורך השיחה - סיכום של 3,000 מילים מפספס את
    מטרתו. העקומה שנוצרת, לפי ~130 מילות דיבור לדקה בעברית ובהנחת צפיפות
    נתונים בינונית (~4% מהמילים מכילות ספרה):

       30 שנ'    ~65 מילים  →   2+ נושאים,    29+ מילים (חסום ע"י תקרת 45%)
        2 דק'   ~260 מילים  →   2+ נושאים,    90+ מילים
       10 דק' ~1,300 מילים  →   5+ נושאים,   314+ מילים
       30 דק' ~3,900 מילים  →   9+ נושאים,   771+ מילים
       60 דק' ~7,800 מילים  →  13+ נושאים, 1,391+ מילים

    ובאותו אורך (30 דק'), לפי צפיפות הנתונים בפועל: שיחה כמעט בלי מספרים
    מקבלת ~499 מילים, ושיחה עמוסת נתונים ~918. זה ההבדל שאורך לבדו מפספס.

    היעד הוא רצפה, לא תקרה (ראה _TASK_SCHEMA_HINT): עדיף סיכום ארוך מהיעד
    על פני סיכום שהשמיט נושא או נתון.
    """
    root = math.sqrt(max(word_count, 1))
    topics = min(max(round(root / 7), 2), 16)
    # גם העומק לכל נושא גדל, לא רק מספר הנושאים: בפגישה ארוכה כל נושא נדון
    # לעומק ומגיע לו יותר משפט אחד.
    words_per_topic = min(max(round(30 + root / 3), 35), 75)
    base = topics * words_per_topic
    # תוספת לפי צפיפות הנתונים. נמדד בבדיקה חיה מול Gemini: המודל מציית ליעד
    # המילים כמעט במדויק, ולכן יעד שנגזר מאורך בלבד הוא בדיוק מה שמפיל את
    # המספרים - בישיבה עם 55 נתונים ב-1,069 מילים שרדו רק 27% מהם, כי פשוט
    # לא היה להם מקום. עם התוספת הזו (ועם ניסוח הרצפה בפרומפט) השרידות עלתה
    # ל-80% בלי אף מספר מומצא. המקדם 2 ולא יותר כדי שהתוספת תבדיל בין
    # צפיפויות לאורך כל הטווח במקום להיחסם מיד בתקרה.
    figures_bonus = min(figure_count * 2, base)
    # תקרת ביטחון להקלטות זעירות: בלי זה הקלטה של חצי דקה (~65 מילים) מקבלת
    # יעד ארוך מהתמלול עצמו, שהוא הזמנה לריפוד ולהמצאת פרטים.
    summary_words = min(base + figures_bonus, max(round(word_count * 0.45), 25))
    return topics, summary_words


def summarize_and_extract_todos(
    segments: list[TranscriptSegment], today_iso: str
) -> tuple[str, str, list[TodoItem]]:
    """מחזיר (כותרת מוצעת, סיכום, TO DO). הכותרת המוצעת משמשת רק כשהמשתמש
    לא הזין כותרת משלו (ראה pipeline.py)."""
    client = genai.Client(api_key=settings.gemini_api_key)

    transcript_text = _format_transcript(segments)
    word_count = _word_count(segments)
    figure_count = _figure_count(segments)
    min_topics, min_words = _summary_budget(word_count, figure_count)

    task_hint = _TASK_SCHEMA_HINT.format(
        today=today_iso,
        word_count=word_count,
        figure_count=figure_count,
        min_topics=min_topics,
        min_words=min_words,
    )

    response = call_with_retry(
        client.models.generate_content,
        model=_MODEL,
        contents=f"{task_hint}\n\nתמלול הפגישה:\n{transcript_text}",
        config=types.GenerateContentConfig(
            system_instruction=_SYSTEM_PROMPT,
            response_mime_type="application/json",
            response_schema=_MeetingSummary,
            # מפורש, מאותה סיבה בדיוק שבתמלול (ראה transcription.py): בלי
            # התקרה המפורשת ה-API חותך את הפלט הרבה לפני יכולת המודל, וסיכום
            # ארוך - שהוא בדיוק מה שפגישה ארוכה מקבלת - חוזר כ-JSON קטוע.
            max_output_tokens=_MAX_OUTPUT_TOKENS,
        ),
    )

    # result.parsed הוא כבר מופע _MeetingSummary מאומת (Pydantic), לא dict/JSON
    # גולמי - כך שאי-התאמה בין הסכמה (list) לקוד שקורא אותה הופכת לבלתי-
    # אפשרית structurally במקום להתגלות רק בזמן ריצה.
    parsed: _MeetingSummary = response.parsed or _MeetingSummary.model_validate_json(
        response.text
    )

    return parsed.title, parsed.summary, parsed.todos
