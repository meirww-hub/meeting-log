# --- שלב 1: MVP ---
fastapi==0.115.0
uvicorn[standard]==0.30.6
python-multipart==0.0.9
pydantic==2.9.2
pydantic-settings==2.5.2
python-dotenv==1.0.1

google-cloud-firestore==2.19.0
google-api-python-client==2.144.0
google-auth==2.34.0
google-auth-oauthlib==1.2.1
# הזרמת קובץ האודיו מ-Drive (GET /recordings/{id}/audio) פונה ל-HTTP הגולמי
# של Drive ולא דרך googleapiclient - ראה services/drive.py:stream_file.
requests==2.32.3

google-genai==1.15.0

# --- שלב 2: זיהוי דוברים ---
# טביעת-קול דרך ONNX Runtime בלבד (לא torch/pyannote.audio - הוחלפו כי הם
# כבדים מדי לקונטיינר). המודל עצמו (wespeaker ResNet34) יורד ב-Dockerfile,
# לא כאן.
sherpa-onnx==1.13.5
numpy==2.2.6
