# תוכנית: Meeting Log - הקלטה, תמלול, סיכום ו-TO DO אוטומטי

**שם המוצר: "Meeting Log"** (לא "MeetingScribe" כפי שהוצע בטיוטה הראשונית -
הוחלף ב-2026-08-07 כדי להתאים לשם פרויקט ה-GCP `meeting-log`).

## הקשר (Context)

המטרה: אפליקציית אנדרואיד שמקליטה שיחות/פגישות, מתמללת אותן תוך זיהוי דוברים
לפי קול (עם למידה מצטברת של שמות דוברים לאורך זמן), מפיקה סיכום איכותי הניתן
לעריכה, ומחלצת ממנו רשימת משימות (TO DO) - כאשר משימות עם תאריכים מוצגות
כציר זמן מסוג גאנט עם אבני דרך. כל התוצרים נשמרים אוטומטית בגוגל דרייב של
המשתמש, בתיקייה שנוצרת לפי תאריך + כותרת הנושא.

זהו פרויקט חדש (תיקייה ריקה, ללא חשבונות/מפתחות API קיימים). המשתמש ביקש
שפת ממשק/תמלול עברית בעיקרה, ושתהליך זיהוי הדוברים יעבוד כך: בהקלטה
הראשונה דוברים לא מזוהים יסומנו "דובר 1", "דובר 2" וכו', המשתמש יזין את
שמם בדיעבד, והמערכת תזהה אותם אוטומטית בהקלטות הבאות. אין העדפת ארכיטקטורה
- הומלץ סטאק Google-first (Drive, Firestore) בשילוב Gemini API (Google AI
Studio) גם לתמלול+diarization וגם לסיכום/חילוץ משימות - הוחלף מ-Claude
(לסיכום) ומ-Google Cloud Speech-to-Text (לתמלול) ב-2026-08-07, כדי לנצל את
השכבה החינמית של Gemini במקום שני שירותים בתשלום/מוגבלים.

## ארכיטקטורה מומלצת

```
[אפליקציית אנדרואיד]
   → מקליטה (MediaRecorder), מעלה קובץ אודיו לשרת עיבוד (Cloud Run)
[שרת עיבוד - Cloud Run, Python]
   1. תמלול + diarization גנרי ("דובר 1/2") באמצעות Gemini API (הבנת אודיו)
   2. (שלב 2) Speaker Embeddings נוספים (pyannote.audio) - למי דיבר מתי
   3. זיהוי דוברים: השוואת embeddings לפרופילים שמורים ב-Firestore;
      דובר לא מוכר → "דובר 1/2"; דובר מוכר → שם אמיתי אוטומטית
   4. Gemini API: סיכום איכותי + חילוץ משימות (JSON: תיאור, אחראי, תאריך)
   5. יצירת תיקייה ב-Drive "YYYY-MM-DD - <נושא>" והעלאת:
      - קובץ האודיו המקורי
      - תמלול מלא (Google Doc, מסומן לפי דובר)
      - סיכום (Google Doc - ניתן לעריכה חופשית ב-Drive)
      - TO DO + גאנט (Google Sheet: טבלת משימות + טאב גאנט חזותי
        לפי תאריכים; ללא תאריכים → רשימת TO DO רגילה)
   6. (אופציונלי) יצירת אירועי אבן-דרך ביומן Google Calendar
[מסך "זיהוי דוברים" באפליקציה]
   אחרי כל הקלטה מציג קטעי שמע קצרים לכל "דובר לא מזוהה" + שדה שם;
   השם ו-embedding הקול נשמרים ב-Firestore לזיהוי אוטומטי בפעם הבאה
```

**רכיבים טכניים מרכזיים:**
- אפליקציית אנדרואיד (Kotlin): הקלטה, ניהול העלאות ברקע (WorkManager),
  Google Sign-In (OAuth ל-Drive/Calendar), מסך תיוג דוברים, מסך היסטוריית
  הקלטות.
- Backend (Python, Cloud Run): pipeline העיבוד המלא (diarization → תמלול
  → זיהוי דוברים → סיכום/משימות → כתיבה ל-Drive).
- Firestore: פרופילי דוברים (שם + וקטור embedding קול), מטא-דאטה של
  הקלטות/סטטוס עיבוד.
- Google Drive API: יצירת תיקיות/קבצים לפי הפורמט המבוקש.
- Gemini API (Google AI Studio, שכבה חינמית): תמלול+diarization עם timestamps
  (audio understanding), סיכום איכותי, וחילוץ משימות/תאריכים במבנה JSON -
  שלושתם דרך אותו מפתח API יחיד.
- pyannote.audio (שלב 2 עתידי): embeddings קוליים לזיהוי דוברים חוצה-הקלטות
  (הפרדת הדוברים הבסיסית כבר מגיעה מ-Gemini משלב 1).
- (אופציונלי, שלב מאוחר) Google Calendar API: דחיפת אבני דרך כאירועים.

**דרישות הקמה מקדימות אצל המשתמש** (לפני תחילת פיתוח בפועל):
- פרויקט Google Cloud חדש + הפעלת Drive API, Firestore
  (Calendar API אופציונלי).
- מפתח Gemini API (Google AI Studio) לתמלול+diarization וגם לסיכום/חילוץ משימות.
- OAuth consent screen + client ID לאפליקציית האנדרואיד.

## שלבי פיתוח (MVP → הרחבה)

**שלב 1 - MVP בסיסי**
- אפליקציית אנדרואיד: הקלטה + שמירה מקומית + העלאה ידנית לשרת.
- Backend: תמלול+diarization גנרי ("דובר 1/2") וגם סיכום/TO DO - שניהם
  דרך Gemini API, ללא גאנט עדיין.
- יצירת תיקיית Drive לפי תאריך+נושא עם תמלול, סיכום, TO DO.

**שלב 2 - זיהוי דוברים לומד**
- מסך תיוג דוברים באפליקציה (שם + השמעת קטע).
- שמירת embeddings ב-Firestore והתאמה אוטומטית בהקלטות עתידיות.

**שלב 3 - גאנט ואבני דרך**
- זיהוי תאריכים בטקסט המשימות (Gemini JSON structured output).
- טאב גאנט חזותי ב-Google Sheet (טווחי תאריכים + אבני דרך).
- (אופציונלי) דחיפת אבני דרך ל-Google Calendar.

**שלב 4 - ליטוש**
- עריכת סיכום ישירות מתוך האפליקציה (או הפניה ל-Google Docs).
- התראות בסיום עיבוד, טיפול בשגיאות/ניסיון חוזר, מסך היסטוריה/חיפוש.

## פיצ'רים נוספים מוצעים (לאישור המשתמש)

1. **גיליון אינדקס מרכזי** ב-Drive שמרכז את כל ההקלטות (תאריך, נושא,
   קישור לתיקייה, סטטוס משימות) - נוח לחיפוש/מעקב. גם ניתן לעריכה ידנית
   מתוך האפליקציה (לא רק דרך Drive).
2. **זיהוי אחראי ותאריך יעד אוטומטי** מתוך ניסוח חופשי ("X יעשה את Y עד
   יום שלישי") - לא רק "יש תאריך/אין תאריך" אלא שיוך משימה לאדם. ניתן
   לתיקון/שיוך ידני באפליקציה במקרה שהזיהוי האוטומטי טעה.
3. **התראות Push** בסיום עיבוד הקלטה (האפליקציה מודיעה "הסיכום מוכן").
4. **תמיכה רב-לשונית** - זיהוי שפה אוטומטי למקרה של שיחות שאינן בעברית.
5. **תיוג/קטגוריות** להקלטות (למשל "עבודה", "אישי", "פרויקט X") לסינון
   מהיר בהיסטוריה, עם אפשרות להוסיף/לערוך תגיות ידנית באפליקציה.
6. **מעקב סטטוס משימות** - סימון "בוצע"/"פתוח" ישירות בגיליון ה-TO DO
   או ידנית באפליקציה, עם אפשרות עתידית לשלוח תזכורת.

## הרחבה עתידית - ריבוי משתמשים (לא כרגע)

המשתמש ציין שבעתיד (לא בשלב הנוכחי) הוא ירצה שהאפליקציה תרוץ על כמה
טלפונים, כשכל משתמש רואה/ניגש רק להקלטות שהטלפון שלו יצר. זה משנה כמה
החלטות ארכיטקטורה שכרגע פשוטות בכוונה לצורך MVP חד-משתמשי:

- **היום**: הבקאנד כותב ל-Drive באמצעות OAuth refresh token יחיד (של
  meirww@gmail.com), ו-Firestore/UploadWorker משתמשים במזהה קבוע
  `primary_user` (ראה `android/.../UploadWorker.kt` וה-`OWNER_USER_ID`).
- **בעתיד**: כל משתמש יצטרך Google Sign-In אמיתי באפליקציה (OAuth client
  מסוג Android, לא Desktop), כדי שהשרת יכתוב להקלטות/דרייב של אותו משתמש
  ספציפית, ו-Firestore יסנן לפי מזהה המשתמש האמיתי (ולא קבוע). זה גם
  ישפיע על `identify_speakers` (פרופילי דוברים כנראה צריכים להיות פר-משתמש
  או משותפים, לפי החלטה עתידית).

אין צורך לפעול על זה עכשיו - רק לזכור בעת תכנון שלב שיתוף/ריבוי משתמשים.

## פיצ'רים נוספים שאושרו (2026-08-07)

7. **סיכום שיחות WhatsApp** - מסלול קלט נוסף: המשתמש משתף (Export Chat)
   קובץ טקסט של שיחת WhatsApp לאפליקציה. מדלג על שלב התמלול (`transcription.py`
   / `speaker_id.py`) ומזין ישירות את `summarize_and_extract_todos` הקיים
   עם הטקסט. תוצר נשמר ב-Drive באותו פורמט תיקייה כמו הקלטה רגילה.
8. **הקלטת שיחות טלפון (Pixel 9 Pro)** - לא נבנה בתוך Meeting Log עצמה.
   נבדק ואושר: **[cally](https://github.com/LyoSU/cally)** (קוד פתוח, ללא
   צורך ב-root, עובד ב-Pixel עם Android נקי דרך Shizuku) הוא הכלי המומלץ
   להקלטת השיחה עצמה. האינטגרציה שלנו: להוסיף ל-Meeting Log Android
   Intent Filter שקולט קבצי אודיו משותפים (`ACTION_SEND`, `audio/*`) - כך
   שאחרי הקלטה ב-cally, המשתמש לוחץ Share → Meeting Log, והקובץ נכנס
   לאותו pipeline קיים (`POST /recordings`) כמו הקלטה רגילה מהאפליקציה.
   סיבת ההחלטה: לא לשכפל תחזוקה של שכבת Shizuku/VOICE_CALL שברירית
   שכבר קיימת ומתוחזקת בפרויקט ייעודי.

## אימות ובדיקה

- בדיקת קצה-לקצה: הקלטת שיחת דמו בעברית עם 2-3 דוברים → אימות שנוצרה
  תיקיית Drive עם השם הנכון, תמלול תקין, סיכום קריא, TO DO/גאנט נכונים.
- בדיקת זיהוי דוברים: הקלטה שנייה עם אותם דוברים לאחר תיוג ידני בפעם
  הראשונה → אימות שהשמות זוהו אוטומטית.
- בדיקת מקרי קצה: הקלטה ללא תאריכים (TO DO בלבד, ללא גאנט), הקלטה עם
  רעש רקע, הקלטה ארוכה (מעל 30 דקות).
